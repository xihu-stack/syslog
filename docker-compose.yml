version: '3.8'
services:
  app:
    build: .
    container_name: ipguard-ai
    network_mode: host
    volumes:
      - ./backend/data:/app/data
      - .:/project          # 挂载git仓库，支持远程 git pull 更新
    restart: unless-stopped
    environment:
      - PYTHONIOENCODING=utf-8
      - PORT=18000
